SUPABASE_URL=https://lgmdjkjwiimyqkmyfnlt.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
ORGANIZATION_ID=your_organization_uuid_here
MCP_API_KEY=generate_a_strong_random_key_here
# REQUIRED: PIN protecting the OAuth approval page. The server refuses to start
# without it. Use a long, random value (min. 8 chars) — this is the only barrier
# between the public internet and a service-role connection to your whole CRM.
OAUTH_PIN=generate_a_long_random_pin_here
# For local dev only (Vercel sets VERCEL_URL automatically)
PORT=3000
